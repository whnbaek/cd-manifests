---
# Keep existing nvidia RuntimeClass for compatibility
# HAMi device plugin works with nvidia runtime
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
  labels:
    app.kubernetes.io/name: nvidia-runtime
handler: nvidia
